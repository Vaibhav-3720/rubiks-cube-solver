#pragma once

#include <cstdint>

// Mathematical utility functions used for cube state indexing.

uint32_t factorial(uint32_t n);

uint32_t choose(uint32_t n, uint32_t k);

uint32_t pick(uint32_t n, uint32_t k);